# Relative path - same origin as the page. Works on any domain (App Platform, droplet, own server).
# The reverse proxy (nginx / App Platform router) forwards /api/* to the backend.
VITE_API_URL=/api
VITE_APP_NAME=Cloud9 ERP
VITE_APP_VERSION=1.0.0
